services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
      secrets:
        - env
    image: sabine-images
    container_name: sabine-images
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env

secrets:
  env:
    file: .env